from django.db import models

# Create your models here.
"""
class signs(models.Model):
    sign_date=models.DateTimeField("Sign date")
    sign_hash=models.CharField(max_length=64)
    sign_filename=models.CharField(max_length=64)
"""
